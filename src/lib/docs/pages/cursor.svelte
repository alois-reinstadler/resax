<script lang="ts">import DemoSection from '../DemoSection.svelte';import {cursor,type CursorVariant} from '$lib/registry/ui/cursor';let enabled=$state(false);let variant=$state<CursorVariant>('glow');const variants:CursorVariant[]=['default','blend','blob','glow','label','magnet','reticle'];</script>
<div class="page-heading"><p class="eyebrow">Attachment</p><h1>Cursor</h1><p>Decorative pointer treatments scoped to exactly the surface you attach them to.</p></div>
<DemoSection title="Bounded preview" source={'<div {@attach cursor({ variant: "glow" })}>...</div>'}><div class="controls"><button onclick={()=>enabled=!enabled}>{enabled?'Disable':'Enable'} custom cursor</button><select bind:value={variant} aria-label="Cursor variant">{#each variants as option}<option value={option}>{option}</option>{/each}</select></div><div class="cursor-stage" {@attach enabled?cursor({variant,label:'Explore'}):undefined}><span>Move inside this preview</span><button data-rx-cursor-label="Open" data-rx-cursor-magnet>Magnetic target</button></div></DemoSection>
<style>.controls{display:flex;gap:.65rem;margin-bottom:1rem}.controls :is(button,select){border:1px solid rgb(var(--rx-text)/.12);border-radius:var(--rx-radius);padding:.55rem .8rem;color:rgb(var(--rx-text));background:rgb(var(--rx-bg))}.cursor-stage{display:grid;place-items:center;gap:1rem;min-height:16rem;border:1px dashed rgb(var(--rx-primary)/.35);border-radius:calc(var(--rx-radius)*1.2);background:radial-gradient(circle at center,rgb(var(--rx-primary)/.11),transparent 65%)}.cursor-stage button{border:0;border-radius:999px;padding:.75rem 1rem;color:rgb(var(--rx-light));background:rgb(var(--rx-primary))}</style>
