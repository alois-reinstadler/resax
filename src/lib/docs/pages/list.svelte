<script lang="ts">import DemoSection from '../DemoSection.svelte';import {List,type ListAction,type ListItem} from '$lib/registry/ui/list';const items:ListItem[]=[{id:'design',title:'Design system',description:'Tokens and foundations'},{id:'components',title:'Components',description:'Reusable Svelte UI'},{id:'docs',title:'Documentation',description:'Guides and examples'},{id:'legacy',title:'Legacy build',disabled:true}];const actions:ListAction[]=[{key:'archive',label:'Archive'},{key:'delete',label:'Delete',tone:'danger',dismiss:true,confirm:true,confirmLabel:'Sure?'}];const leadingActions:ListAction[]=[{key:'pin',label:'Pin',tone:'success'}];let selected=$state<string|string[]>('components');let action=$state('Swipe a row or use its arrow keys');let order=$state(items.map(item=>item.title).join(' · '));const variants=['default','cards','glow','hover','reveal','stripe'] as const;</script>
<div class="page-heading"><p class="eyebrow">Component</p><h1>List</h1><p>Structured, selectable content with the source swipe trays, full-swipe removal, confirmation and pointer reordering.</p></div><DemoSection title="Selection"><div class="demo"><List {items} bind:selected selectionMode="single" ariaLabel="Documentation sections" variant="glow"/><p>Selected: {selected}</p></div></DemoSection><DemoSection title="Swipe actions and reorder"><div class="demo"><List {items} {actions} {leadingActions} swipeToDelete reorderable threshold={0.45} fullSwipeRatio={1.8} onAction={(item,value)=>action=`${value.label}: ${item.title}`} onRemove={(item)=>action=`Removed: ${item.title}`} onReorder={(value)=>order=value.map(item=>item.title).join(' · ')}/><p>{action}</p><p>Order: {order}</p></div></DemoSection><DemoSection title="Variants"><div class="grid">{#each variants as variant}<div><code>{variant}</code><List {items} {variant}/></div>{/each}</div></DemoSection><style>.demo{width:min(100%,34rem)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(18rem,1fr));gap:2rem}.grid>div{display:grid;gap:.7rem}</style>
