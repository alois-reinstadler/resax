<script lang="ts">import DemoSection from '../DemoSection.svelte';import {Scrollbar} from '$lib/registry/ui/scrollbar';const rows=Array.from({length:18},(_,i)=>`Navigation surface ${i+1}`);const variants=['default','dots','glow','gradient','minimal','rounded'] as const;</script>
<div class="page-heading"><p class="eyebrow">Component</p><h1>Scrollbar</h1><p>A native scrolling surface with smoothed thumb tracking, a slower ghost, spring overscroll, delayed hiding, pointer-capture dragging, and all six source skins.</p></div>
<DemoSection title="Source variants" source={'<Scrollbar variant="dots" autoHide={false} smoothness={0.18} ghost overscroll>...</Scrollbar>'}><div class="gallery">{#each variants as variant}<article><code>{variant}</code><Scrollbar {variant} color="primary" autoHide={false} ariaLabel={`${variant} scrollbar preview`}><div class="scroll-content">{#each rows.slice(0,12) as row}<p>{row}</p>{/each}</div></Scrollbar></article>{/each}</div></DemoSection>
<DemoSection title="Modes and behavior" source={'<Scrollbar mode="embedded" orientation="both" size="lg" autoHide smooth ghost ghostGradient overscroll />'}><dl><div><dt><code>mode</code></dt><dd><code>embedded</code> owns its viewport, <code>bare</code> removes the shell, and <code>page</code> follows document scrolling while hiding the native page bar.</dd></div><div><dt><code>variant / color / color2</code></dt><dd>Six source skins, token accent, and the gradient variant's second stop.</dd></div><div><dt><code>orientation</code></dt><dd>vertical · horizontal · both</dd></div><div><dt><code>smoothness / smooth</code></dt><dd>The source default is 0.18 thumb interpolation; <code>smooth</code> controls track-click scrolling.</dd></div><div><dt><code>ghost / ghostGradient / overscroll</code></dt><dd>A 0.42-rate ghost with optional gradient and a 22px/.65 damped edge spring.</dd></div><div><dt><code>intensity / showTrack</code></dt><dd>Glow strength and rounded-track capsule visibility.</dd></div><div><dt><code>hideDelay / autoHide</code></dt><dd>Custom tracks fade, shrink to .35, and blur after 900ms unless hovered or dragged.</dd></div><div><dt><code>size / maxHeight / ariaLabel</code></dt><dd>Geometry and accessible region labeling. Source demo-only <code>lines</code> maps to consumer-provided children.</dd></div></dl></DemoSection>
<style>.gallery{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem}.gallery article{min-width:0;padding:1rem;border:1px solid rgb(var(--rx-text)/.08);border-radius:16px}.gallery article>code{display:block;margin-bottom:.6rem;color:color-mix(in srgb,rgb(var(--rx-color)) 78%,rgb(var(--rx-text)))}:global(.gallery .rx-scrollbar){width:100%;height:13rem}.scroll-content{padding:.4rem 1rem}.scroll-content p{margin:0;padding:.55rem;border-bottom:1px solid rgb(var(--rx-text)/.08)}dl{display:grid;gap:.65rem}dl div{display:grid;grid-template-columns:minmax(12rem,.35fr) 1fr;gap:1rem;padding:.65rem;border-bottom:1px solid rgb(var(--rx-text)/.08)}dt,dd{margin:0}@media(max-width:720px){.gallery{grid-template-columns:1fr}dl div{grid-template-columns:1fr}}</style>
