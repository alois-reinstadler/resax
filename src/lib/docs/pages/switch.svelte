<script lang="ts">import{Check,X}from'@lucide/svelte';import DemoSection from'../DemoSection.svelte';import{Switch}from'$lib/registry/ui/switch';let base=$state(false),day=$state(false),dot=$state(true),glow=$state(true),label=$state(false),liquid=$state(false),material=$state(true);</script>
<div class="page-heading"><p class="eyebrow">Component</p><h1>Switch</h1><p>Elastic drag, day/night scenery, dot feedback, neon breathing, labels, liquid trails, and Material scaling.</p></div>
<DemoSection title="Base elastic switch"><div class="switch-row"><Switch variant="base" glow bind:checked={base} aria-label="Elastic switch">{#snippet on()}<Check size={11}/>{/snippet}{#snippet off()}<X size={11}/>{/snippet}</Switch><span>Press, drag, or click: {base?'on':'off'}</span></div></DemoSection>
<DemoSection title="Source variants"><div class="switch-grid"><label>Day / night <Switch variant="day-night" bind:checked={day} aria-label="Day night switch"/></label><label>Dot <Switch variant="dot" bind:checked={dot} aria-label="Dot switch"/></label><label>Glow <Switch variant="glow" bind:checked={glow} aria-label="Glow switch"/></label><label>Label <Switch variant="label" bind:checked={label} aria-label="Label switch"/></label><label>Liquid <Switch variant="liquid" bind:checked={liquid} aria-label="Liquid switch"/></label><label>Material <Switch variant="material" bind:checked={material} aria-label="Material switch"/></label></div></DemoSection>
<DemoSection title="Sizes, shapes, and states"><div class="switch-row"><Switch checked size="sm" shape="square" aria-label="Small square switch"/><Switch checked size="lg" shape="rounded" color="success" aria-label="Large rounded switch"/><Switch loading checked aria-label="Loading switch"/><Switch disabled aria-label="Disabled switch"/></div></DemoSection>
<style>.switch-row{display:flex;align-items:center;gap:1rem;flex-wrap:wrap}.switch-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:1.5rem}.switch-grid label{display:flex;align-items:center;justify-content:space-between;gap:1rem;color:rgb(var(--rx-text));font-weight:600}</style>
