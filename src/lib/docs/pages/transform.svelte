<script lang="ts">import DemoSection from'../DemoSection.svelte';import{Transform}from'$lib/registry/ui/transform';let legacy=$state(true);const variants=['morph','fade','flip','scale','slide','expand'] as const;</script>
<div class="page-heading"><p class="eyebrow">Component</p><h1>Transform</h1><p>The source trigger-and-panel family, with distinct overlay, 3D hinge, spring pop, directional reveal, and top-down wipe contracts.</p></div>
<DemoSection title="Source panel variants" source={'<Transform label="Open panel" variant="morph" placement="anchor" side="over" align="start" speed={1} glow>...</Transform>'}><div class="variants">{#each variants as variant}<article><code>{variant}</code><Transform label={`Open ${variant}`} {variant} axis="x" from="bottom" placement="anchor" side="over" align="start" speed={1} glow bounce softFade><div class="panel"><strong>{variant} panel</strong><p>The panel owns the source enter and leave choreography.</p></div></Transform></article>{/each}</div></DemoSection>
<DemoSection title="Legacy wrapper compatibility" source={'<Transform bind:show variant="scale">...</Transform>'}><button class="toggle" onclick={()=>legacy=!legacy}>Toggle wrapper</button><Transform bind:show={legacy} variant="scale"><div class="panel">Generic child transitions remain available for existing installs.</div></Transform></DemoSection>
<style>.variants{display:flex;flex-wrap:wrap;gap:1rem}.variants article{display:grid;gap:.6rem}.panel{display:grid;gap:.5rem;min-width:260px}.panel p{margin:0;color:rgb(var(--rx-text-muted))}.toggle{margin-bottom:1rem;border:1px solid rgb(var(--rx-border));border-radius:var(--rx-radius);padding:.65rem 1rem;background:rgb(var(--rx-surface-2));color:rgb(var(--rx-text))}</style>
