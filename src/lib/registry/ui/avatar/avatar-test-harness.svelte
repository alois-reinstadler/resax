<script lang="ts">import { Avatar, AvatarGroup } from './index';import type { AvatarGroupProps,AvatarProps } from './index';let { mode = 'single', max = 2, badge, variant = 'base' }: { mode?: string; max?: number; badge?: string | number | boolean;variant?:AvatarProps['variant']|AvatarGroupProps['variant'] } = $props();</script>
{#if mode === 'group'}<AvatarGroup {max} variant={variant as AvatarGroupProps['variant']}><Avatar fallback="A" /><Avatar fallback="B" /><Avatar fallback="C" /><Avatar fallback="D" /></AvatarGroup>{:else}<Avatar src={mode === 'broken' ? '/not-found.png' : undefined} fallback="RX" alt="Resax avatar" {badge} variant={variant as AvatarProps['variant']} />{/if}
