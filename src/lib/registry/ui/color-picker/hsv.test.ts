import { describe, expect, it } from 'vitest'; import { hsvToHex, parseColor, rgbToHsv } from './hsv';
describe('HSV conversion',()=>{it.each(['#000000','#ffffff','#ff0000','#00ff00','#0000ff','#808080','#123456','#abcdef'])('round-trips %s',(hex)=>expect(hsvToHex(parseColor(hex)!)).toBe(hex));it('preserves the last hue for greys, black, and white',()=>{expect(rgbToHsv(0,0,0,1,217).h).toBe(217);expect(rgbToHsv(255,255,255,1,217).h).toBe(217);expect(rgbToHsv(128,128,128,1,217).h).toBe(217)});it('round-trips alpha edges',()=>{expect(hsvToHex(parseColor('#ff000000')!,true)).toBe('#ff000000');expect(hsvToHex(parseColor('#ff0000ff')!,true)).toBe('#ff0000ff')})});
