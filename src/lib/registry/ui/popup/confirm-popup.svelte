<script lang="ts" module>import type{RxColor}from'$lib/registry/lib/color';export interface ConfirmPopupProps{open?:boolean;title?:string;message:string;confirmLabel?:string;cancelLabel?:string;destructive?:boolean;color?:RxColor;preventClose?:boolean;onConfirm?:()=>void;onCancel?:()=>void;onOpenChange?:(open:boolean)=>void}</script>
<script lang="ts">import Popup from './popup.svelte';import{Button}from'../button';let{open=$bindable(false),title='Confirm',message,confirmLabel='Confirm',cancelLabel='Cancel',destructive=false,color,preventClose=false,onConfirm,onCancel,onOpenChange}:ConfirmPopupProps=$props();function cancel(){open=false;onCancel?.()}function confirm(){open=false;onConfirm?.()}</script><Popup bind:open {title} color={color??(destructive?'danger':'primary')} transition="confirm" confirm closeHidden {preventClose} {onOpenChange}><p>{message}</p>{#snippet footer()}<Button variant="border" onclick={cancel}>{cancelLabel}</Button><Button color={destructive?'danger':color} style={destructive?'color:hsl(from rgb(var(--rx-color)) h 35% 8%)':'color:var(--rx-color-foreground)'} onclick={confirm}>{confirmLabel}</Button>{/snippet}</Popup>
